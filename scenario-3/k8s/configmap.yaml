apiVersion: v1
kind: ConfigMap
metadata:
  name: inference-api-config
  namespace: scenario-3
data:
  DATABASE_URL: "postgresql://app_user:s3cr3tP@ss@db.internal:5432/inference"
  CACHE_TTL: "300"
  MAX_CONNECTIONS: "10"
  LOG_FORMAT: "json"
  APP_VERSION: "1.0.0"
  ENVIRONMENT: "production"
